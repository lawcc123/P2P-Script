# P2P-Script

`P2P_Registry_Sync.py` updates the cheque registry workbook with P2P status, IR number, cheque number, and cheque date.

The record will only be updated once it has invoice and PO number


`P2P_PO_Create.py` creates Yardi P2P purchase orders from rows in the cheque registry workbook.

